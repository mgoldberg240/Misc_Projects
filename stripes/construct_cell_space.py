from numpy import genfromtxt
import operator
import unit
import shift
import cell_plot

unit = unit.unit
shift = shift.shift
plot_cells = cell_plot.plot_cells

def construct_cell_space(string):
	for i in string:

		if i in ['a','A']:
			np.hstack(CSA
			[1,0,1]
			[1,1,1]
			[1,0,1]
			[1,0,1]